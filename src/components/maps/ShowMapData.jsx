import { Typography } from "@mui/material"

const ShowMapData = ({
    geojson
}) => {
    return (
        <Typography>
            Panel de datos del mapa
        </Typography>
    )
}

export default ShowMapData